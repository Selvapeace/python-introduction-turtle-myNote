# 列表
mix = [1,'turtle',3.14,[6,6,6]]
empty = []
mix.append('福禄娃娃')   # 向列表添加元素，一次只能加一个元素（但可以是一个列表，加入后表现为列表中的列表）
mix.extend(['竹林小溪','hhh'])     # 用列表形式添加多个元素
mix.insert(0,'牡丹')            # 把牡丹插入到列表的第一个
